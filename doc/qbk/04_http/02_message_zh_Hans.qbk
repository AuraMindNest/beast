[/
    Copyright (c) 2016-2019 Vinnie Falco (vinnie dot falco at gmail dot com)

    Distributed under the Boost Software License, Version 1.0. (See accompanying
    file LICENSE_1_0.txt or copy at http://www.boost.org/LICENSE_1_0.txt)

    Official repository: https://github.com/boostorg/beast
]

[section 消息容器]

Beast 提供了一个类模板 __message__ 和一些别名，用于建模 HTTP/1 和 [@https://tools.ietf.org/html/rfc7540 HTTP/2] 消息：

[table 消息
[[名称][描述]]
[[__message__][
    ```
/// HTTP 消息
template<
bool isRequest,             // `true` 表示请求，`false` 表示响应
class Body,                 // 控制用于正文的容器和算法
class Fields = fields>      // 存储字段的容器类型
class message;    ```
]]
[[[link beast.ref.boost__beast__http__request `request`]][
    ```
/// 典型的 HTTP 请求
template<class Body, class Fields = fields>
using request = message<true, Body, Fields>;    ```
]]
[[[link beast.ref.boost__beast__http__response `response`]][
    ```
/// 典型的 HTTP 响应
template<class Body, class Fields = fields>
using response = message<false, Body, Fields>;    ```
]]
]

该容器提供值语义（包括移动和复制，前提是 __Body__ 和 __Fields__ 支持）。用户定义的模板函数参数可以接受任何消息，或使用部分特化仅接受请求或响应。默认的 __fields__ 是一个提供的关联容器，使用标准分配器并支持字段的修改和检查。根据 __rfc7230__，字段名称使用不区分大小写的比较。用户可自定义字段类型。`Body` 类型决定了用于表示正文的容器类型，以及将缓冲区传入/传出容器的算法。该库附带一组常见的正文类型。与字段类似，用户也可自定义正文类型。

有时仅需处理标头。Beast 提供了一个类模板 __header__ 和一些别名，用于建模 HTTP/1 和 HTTP/2 标头：

[table 标头
[[名称][描述]]
[[__header__][
    ```
/// HTTP 标头
template<
bool isRequest,             // `true` 表示请求，`false` 表示响应
class Fields = fields>      // 存储字段的容器类型
class header;    ```
]]
[[[link beast.ref.boost__beast__http__request_header `request_header`]][
    ```
/// 典型的 HTTP 请求标头
template<class Fields>
using request_header = header<true, Fields>;    ```
]]
[[[link beast.ref.boost__beast__http__response_header `response_header`]][
    ```
/// 典型的 HTTP 响应标头
template<class Fields>
using response_header = header<false, Fields>;    ```
]]
]

请求和响应共享版本、字段和正文，但各有少量类型特有的成员。这是通过将标头类声明为 `isRequest` 的部分特化来实现的。__message__ 派生自 __header__；消息可作为参数传递给接受适当类型标头的函数。此外，`header` 公开派生自 `Fields`；消息继承了 `Fields` 的所有成员函数。下图展示了标头与消息之间的继承关系，以及每个部分特化中成员的一些显著差异：

[$beast/images/message.png [width 730px] [height 459px]]

[heading:body 正文类型]

Beast 定义了 __Body__ 概念，它决定了 [link beast.ref.boost__beast__http__message.body `message::body`] 成员的类型（如上图所示），还可能包含缓冲区传输的算法。这些算法在解析和序列化期间使用。用户可以定义满足要求的自定义正文类型，或使用库附带的类型：

[table
[[Name][Description]]
[[[link beast.ref.boost__beast__http__buffer_body `buffer_body`]][一种正文，其 [link beast.ref.boost__beast__http__buffer_body__value_type `value_type`] 持有指向调用者提供缓冲区的原始指针和大小。这允许序列化来自外部源的正文数据，并使用固定大小的缓冲区增量解析消息正文内容。]]
[[[link beast.ref.boost__beast__http__dynamic_body `dynamic_body`]

[link beast.ref.boost__beast__http__basic_dynamic_body `basic_dynamic_body`]][一种正文，其 `value_type` 是 __DynamicBuffer__。它继承了底层动态缓冲区选择的插入复杂度。具有此正文类型的消息可被序列化和解析。]]
[[[link beast.ref.boost__beast__http__empty_body `empty_body`]][一种特殊的正文，具有空的 `value_type`，表示消息无正文。具有此正文的消息可被序列化和解析；但在解析时接收到的正文八位字节将产生唯一错误。]]
[[[link beast.ref.boost__beast__http__file_body `file_body`]

[link beast.ref.boost__beast__http__basic_file_body `basic_file_body`]][此正文由为读/写而打开的文件表示。具有此正文的消息可被序列化和解析。HTTP 算法将使用打开的文件进行读写，以实现流式传输和增量发送/接收。]]
[[[link beast.ref.boost__beast__http__span_body `span_body`]][一种正文，其 `value_type` 是 [@boost:/libs/core/doc/html/core/span.html `span`]（对单个线性字节缓冲区的非拥有引用）。具有此正文类型的消息可被序列化和解析。]]
[[[link beast.ref.boost__beast__http__string_body `string_body`]

[link beast.ref.boost__beast__http__basic_string_body `basic_string_body`]][一种正文，其 `value_type` 是 `std::basic_string` 或 `std::string`。插入复杂度为均摊常数时间，容量呈几何级增长。具有此正文类型的消息可被序列化和解析。这是示例中使用的正文类型。]]
[[[link beast.ref.boost__beast__http__vector_body `vector_body`]][一种正文，其 `value_type` 是 `std::vector`。插入复杂度为均摊常数时间，容量呈几何级增长。具有此正文类型的消息可被序列化和解析。]]
]

[heading 用法]

以下示例展示如何创建和填充请求与响应对象：这里我们构建一个 [@https://tools.ietf.org/html/rfc7231#section-4.3.1 HTTP GET] 请求，其消息正文为空：

[table 创建请求
[[语句] [序列化结果]]
[[[http_snippet_2]][
```
GET /index.htm HTTP/1.1\r\n
Accept: text/html\r\n
User-Agent: Beast\r\n
\r\n```
]]
]

在此代码中，我们创建了一个状态码表示成功的 HTTP 响应。此消息的正文具有非零长度。函数 [link beast.ref.boost__beast__http__message.prepare_payload `message::prepare_payload`] 会根据 `body` 成员的内容和类型自动设置 Content-Length 或 Transfer-Encoding 字段。此函数的使用是可选的；这些字段也可显式设置。

[table 创建响应
[[语句] [序列化结果]]
[[[http_snippet_3]][
```
HTTP/1.1 200 OK\r\n
Server: Beast\r\n
Content-Length: 13\r\n
\r\n
Hello, world!```
]]
]

实现会在序列化消息时根据状态码自动填充过时的 [@https://tools.ietf.org/html/rfc7230#section-3.1.2 reason-phrase]。也可直接使用 [link beast.ref.boost__beast__http__header.reason.overload2 `header::reason`] 设置。

[endsect]
